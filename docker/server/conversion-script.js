import fetch from 'node-fetch';

async function main() {

  const data = [{
    "fileUrl": "",
    "folderId": ""
  }]
}

main()
  .then(() => process.exit(0))
  .catch((error) => {
    console.error(error);
    process.exit(1);
  });